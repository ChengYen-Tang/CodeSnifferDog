The following system-controlled user data identifies the current task scope and the number of verified incoming issues.
Use `ListCurrentFlowIssues` and `GetCurrentFlowIssue` to read the complete incoming issue set before merging it into the repository-level issue set for the same rule.
